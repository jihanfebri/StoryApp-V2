<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Story" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orangeText</item>
        <item name="colorSecondaryVariant">@color/orangeText</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!--        <item name="android:windowActivityTransitions">true</item>-->
        <!--        <item name="android:windowEnterTransition">@android:transition/slide_right</item>-->
        <!--        <item name="android:windowExitTransition">@android:transition/slide_left</item>-->
    </style>

    <style name="Theme.Story" parent="Base.Theme.Story" />

</resources>